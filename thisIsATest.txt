This is a test project for setting up the server properly

Heres how the workflow shoud go.
1) do local work
2) git push to the cluster 
    $ git push cluster master

3) the script to get a notebook window
4) work in the notebook




Setting things up: ( this is stupid, but it works....)
1) Make the cluster remote:
    remote $ git init --bare .git

2) setup local remote:
    local $ git remote add cluster ssh://LR93@orchestra.med.harvard.edu/home/lr93/MYDIR

3) push to cluster:
    local $ git push cluster master

4) Convert the remote from a bare to a working repository:
    remote $ git init
    remote $ git reset --hard




